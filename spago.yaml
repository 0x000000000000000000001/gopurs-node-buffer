package:
  name: "node-buffer"
  dependencies:
    - arraybuffer-types
    - effect
    - maybe
    - st
    - unsafe-coerce
    - nullable
  test:
    main: Test.Main
    dependencies:
      - assert
      - console
      - foldable-traversable
